{
  "name": "@booking/e2e",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end + API contract tests for the Booking Platform.",
  "scripts": {
    "test:e2e": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(e2e) type-checked via tsc\""
  },
  "devDependencies": {
    "@booking/db": "workspace:*",
    "@types/node": "^22.9.0",
    "playwright": "1.63.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
